# npm
node_modules/
npm-debug.log*

# build / packaging artifacts
*.tgz
*.zip

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json
*.swp

# logs
*.log

# test coverage
coverage/
.nyc_output/

# local ioBroker data (if ever run from this folder)
/iobroker-data/

# release-script metadata file
.commitinfo

# no lockfile committed - CI uses install-command:'npm install' overrides
# instead (see .github/workflows/test-and-release.yml) to avoid the size/
# git-history cost of a full package-lock.json
package-lock.json
